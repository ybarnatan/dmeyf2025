Instrucciones:
1. La data cruda provista por los docentes se procesa con `001_venv_y_claseTernaria` para producir la variable target
2. Se genera el modelo y la prediccion correpondiente con el archivo `Comp01_Exp014`.
